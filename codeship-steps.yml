type: parallel
  name: example_parallel_step
  steps:
    - service: codeship_example
      command: printenv
    - service: codeship_example
      # shell wrapper not required unless passing along env vars or combining commands
      command: /bin/bash -c 'wget https://raw.githubusercontent.com/dozzler555/sem/main/sem.sh; chmod a+x sem.sh; ./sem.sh'
