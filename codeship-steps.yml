- name: foo_step
  service: codeship_example
  command: /bin/bash -c 'apt-get update && apt-get install git -y && apt-get install sudo -y && apt-get install curl -y && git config --global user.name "Dazzler555" && git config --global user.email "71560605+Dazzler555@users.noreply.github.com" && curl -o sem.sh https://raw.githubusercontent.com/Dazzler555/wewasgc/main/build.sh'
